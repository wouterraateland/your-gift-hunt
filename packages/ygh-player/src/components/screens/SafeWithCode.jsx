import React from 'react'
import styled from 'styled-components'

const SafeWithCode = styled.div`

`

const SafeWithCodeScreen = ({ isOpen }) => {
  return (
    <SafeWithCode isOpen={isOpen} />
  )
}

export default SafeWithCodeScreen
